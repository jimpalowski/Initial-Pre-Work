// We start the iteration at 1 and loop through until i is greater than or equal to 5 adding to the new array.

// Example
var ourArray = [];

for (var i = 0; i < 5; i++) {
  ourArray.push(i);
}

// Setup
var myArray = [];
for (var i = 1; i <= 5; i++){
  myArray.push(i);
}
// Only change code below this line.

